apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: readiness
  name: readiness
spec:
  revisionHistoryLimit: 3
  replicas: 2
  selector:
    matchLabels:
      app: readiness
  template:
    metadata:
      labels:
        app: readiness
    spec:
      containers:
        - name: color
          image: lrochette/color:yellow
          imagePullPolicy: Always
          ports:
            - containerPort: 8080

          readinessProbe:
            httpGet:
              port: 80
            initialDelaySeconds: 3
            periodSeconds: 3
