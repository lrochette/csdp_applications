apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: globex-prod
spec:
  ingressClassName: nginx
  rules:
    - host: lrcsdp.support.cf-cd.com
      http:
        paths:
          - path: /globex-prod
